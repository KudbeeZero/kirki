{
  "name": "@simcoin/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting runtime utilities (logging, redis, errors, money, config, chain adapter contracts) shared by every Simcoin service.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@simcoin/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.6.0"
  },
  "files": [
    "dist"
  ]
}
